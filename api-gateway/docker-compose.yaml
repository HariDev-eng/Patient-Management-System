services:
  api-gateway:
    build: .
    image: api-gateway:latest
    container_name: api-gateway
    ports:
      - "4004:4004"
    networks:
      - internal

networks:
  internal:
    external: true