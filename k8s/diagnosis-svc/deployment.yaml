apiVersion: apps/v1
kind: Deployment
metadata:
  name: diagnosis-service
  namespace: patient-management
spec:
  replicas: 1
  selector:
    matchLabels:
      app: diagnosis-service
  template:
    metadata:
      labels:
        app: diagnosis-service
    spec:
      containers:
        - name: diagnosis-service
          image: haripradap09/diagnosis-service:1
          ports:
            - containerPort: 4009
          env:
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: DIAGNOSIS_DB_URL
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_USERNAME
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_PASSWORD
            - name: SPRING_JPA_HIBERNATE_DDL_AUTO
              value: update
          resources:
            requests:
              memory: 256Mi
              cpu: 150m
            limits:
              memory: 384Mi
              cpu: 300m